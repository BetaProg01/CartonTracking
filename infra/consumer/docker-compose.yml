version: '3'

services:
  python:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: python
    command: python3 consumer.py
    volumes:
      - ../../kafka-python:/app
    network_mode: host